# Mock Test Project Workspace

This is an isolated test workspace designed for testing the **M.I.K.E.** VS Code extension.

### Structure
- `.agent/skills/`: Contains workspace-level skills (`audio_design`, `react_components`).
- `src/`: Sample source files (`calculator.ts`, `utils.ts`) ready for inspection, reading, and diff-modifications.
